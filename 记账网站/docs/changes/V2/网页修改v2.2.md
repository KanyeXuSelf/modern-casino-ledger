需要实现的功能：

1. 往期账单：
   1. 账单可以删除
   2. 往期账单的详细信息页应该写的是如下内容
      1. 净利润
      2. Hourly抽水（只计算抽水，不计算任何成本）
      3. Hourly净利润
      4. 每个合伙人应得的分水，成本（可以编辑），收账/垫帐数额（可以编辑），实际应到手利润（如果合伙人上桌打牌了，那么这里应该加上其在桌上的输赢）和合伙人和局里的账务（即这场局里应该支付给其多少钱，如果其盈利数额加上赢得的分水为负数，则其应该支付给局里这部分金钱，令这个负数为红色）
   3. 结账记录部分，点开结账部分应该所有玩家的内容都是信息简写，只有点开某个玩家才会出现可以编辑的结账数额相关的内容。
2. 实时记录：
   1. 加入Player的时候加入一个功能，输入player姓名的时候在下方自动出现检索栏，检索栏内出现相关的玩家姓名，这样记录的时候可以快速加入玩家。且加入player的界面中，默认初始buyin是3000（但是保留修改这个默认值的能力）
3. 数据统计：
   1. 做一个玩家信息合并的功能，有时候会记错某些玩家，比如Blue可能记成Blues，我们希望如果发现这样的情况，可以主动在数据统计中整体修改并合并两个玩家的数据。例如：有2条Blue的记录，有7局Blues的记录，但是这两个人是同一个人，我可以把这两个名字统一合并成Blue，并且综合两个名字的数据放在Blue名下
